val i = 5
val count = ref 1
val n = ref i
val main =
  while (!n > 0) {
   count := !count * !n;
   n := !n - 1
  };
  print_string ("fact of ");
  print_int (i);
  print_string (" is ");
  print_int (!count);
  print_string ("\n")