type list = Nil | Cons (int, list)

fun print_list (l) =
  l ? {
  | Nil => print_string ("\n")
  | Cons (h, t) =>
      print_int (h);
      print_string (" ");
      print_list (t)
  }

val main = print_list (Cons (3, Cons (5, Cons (7, Nil))))
