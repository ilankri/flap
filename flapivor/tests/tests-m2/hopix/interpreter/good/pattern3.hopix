val a = A
val b1 = B (1)
val c12 = C (1, 2)
val dab = D (a, b1)

val main =
  (B (2)) ? {
  | B (x) => (
    print_string ("B of ");
    print_int (x);
    print_string ("!\n")
  )
  | _ => print_string ("something else!\n")
  }