type list ('a) = Nil | Cons ('a, list ('a))

fun singleton['a] (l : list ('a)) : bool =
  l ?
  | Cons (_ : 'a, Nil) => true
  | _ : list ('a) => false

val main =
  if singleton[int] (Nil[int]) then
    print_string ("true")
  else
    print_string ("false")
